import React, { Component } from 'react'

export default class ProductCard extends Component {
  render() {
    const {id,gallery,currency,prices,name, inStock} = this.props
    return (
     <>
     </>
    )
  }
}
